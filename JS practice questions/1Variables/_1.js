// Write a JavaScript program to declare a variable named city and assign it your favorite city's name
const city = "vadodara";

// Declare a variable named age and initialize it with your age.
const age = 22

// Declare four variables, one with a string value, one with a number, one with a boolean, and one as null. Log all variables to the console.
const name = "sakshi"
const bool= true
console.log(city,age,name,bool);
